{
  "compilerOptions": {
    "baseUrl": ".", // ← FALTA ESTO (OBLIGATORIO)
    "paths": {
      "@/*": ["src/*"] // ← quita el "./" para evitar ambigüedades
    }
  },
  "exclude": ["node_modules", "dist"]
}
